// DEPRECATED: the size ramp in src/arrange/Arrange.jsx replaced proposals.
// Running this would fight the ramp — kept only for git history.
console.log('deprecated — sizes now come from the progressive ramp in Arrange.jsx');
